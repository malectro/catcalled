<h1>Edit <%=@participant.name%>'s Exit Interview</h1>

<%= form_for([:admin, @participant, @exit_interview]) do |f| %>
  <div><%= notice %></div>

  <%= f.text_area :text %>

  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_participants_path, class: 'back' %>
