<h1>Edit <%=@participant.name%>'s Introduction</h1>

<%= form_for([:admin, @intro], url: admin_participant_intro_path) do |f| %>
  <div><%= notice %></div>

  <%= f.text_area :text %>

  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_participants_path, class: 'back' %>
